<Page
    x:Class="Nukepayload2.N2Engine.Shell.Views.AboutPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Nukepayload2.N2Engine.Shell"
    xmlns:n2ui="using:Nukepayload2.UI.UWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    DataContext="{StaticResource AboutViewModel}"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <Button Style="{ThemeResource NavigationBackButtonNormalStyle}" Click="BtnBack_Click"></Button>
            <TextBlock FontSize="26">关于</TextBlock>
        </StackPanel>
        <n2ui:PropertyGrid Grid.Row="1" Target="{Binding}"/>
    </Grid>
</Page>
